@model IEnumerable<AutoNuoma.Models.Miestas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pavadinimas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pavadinimas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { salis=item.salis }) |
            @Html.ActionLink("Details", "Details", new { salis=item.salis }) |
            @Html.ActionLink("Delete", "Delete", new { salis=item.salis })
        </td>
    </tr>
}

</table>
